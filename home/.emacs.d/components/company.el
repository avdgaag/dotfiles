;;; company.el --- set up company

;;; Commentary:

;;; Code:

(setq company-idle-delay 0.5)
(setq company-tooltip-limit 10)
(setq company-minimum-prefix-length 2)
(global-company-mode 1)

;;; company.el ends here
