# name: class ... extends React.Component
# key: reactx
# contributor: Arjan van der Gaag
# --
/*
 * ${3:Class description}
 */
class ${1:`(let ((fn (capitalize (file-name-nondirectory
                               (file-name-sans-extension
                               (or (buffer-file-name)
                                   (buffer-name (current-buffer))))))))
           (replace-regexp-in-string "_" "" fn t t))`} extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    $0
  }
}